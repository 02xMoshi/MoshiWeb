const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

app.use(express.json());

// Basic route
app.get('/', (req, res) => {
  res.send('Discord Bot Sales Site Backend');
});

app.listen(port, () => {
  console.log(`Server running on port ${port}`);
});
